<nav class="top-nav" aria-label="Principal">
    <a class="brand" href="/" aria-label="Inicio">Casilda <span>y</span> Rodrigo</a>

    <div class="links">
        <a class="link" href="/">Inicio</a>
        <a class="link" href="#ubicacion" id="nav-ubicacion">Ubicación</a>
        <a class="link" href="#rsvp" id="nav-asistencia">Asistencia</a>
        <a class="link" href="#lista" id="nav-lista">Lista</a>
    </div>
</nav>

<script is:inline>
    (function () {
        const isHome = location.pathname === "/" || location.pathname === "";
        if (isHome) return;

        const ids = ["nav-ubicacion", "nav-asistencia", "nav-lista"];
        ids.forEach((id) => {
            const a = document.getElementById(id);
            if (a) a.href = "/" + a.getAttribute("href");
        });
    })();
</script>

<style>
    .top-nav {
        position: sticky;
        top: 0;
        z-index: 30;
        display: grid;
        padding: 12px 14px 9px;
        background: rgba(246, 243, 238, 0.9);
        border-bottom: 1px solid rgba(47, 51, 45, 0.09);
        backdrop-filter: blur(18px);
    }

    .brand {
        display: none;
        justify-self: center;
        color: var(--ink);
        font-family: var(--font-display);
        font-size: 1.26rem;
        font-weight: 300;
        line-height: 1;
        text-decoration: none;
        white-space: nowrap;
    }

    .brand span {
        color: var(--sage);
        font-size: 0.8em;
        font-style: italic;
    }

    .links {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: clamp(12px, 4vw, 20px);
        min-width: 0;
        overflow-x: auto;
        scrollbar-width: none;
    }

    .links::-webkit-scrollbar {
        display: none;
    }

    .link {
        flex: 0 0 auto;
        color: rgba(47, 51, 45, 0.48);
        font-family: var(--font-body);
        font-size: 0.56rem;
        font-weight: 300;
        letter-spacing: 0.14em;
        text-decoration: none;
        text-transform: uppercase;
        white-space: nowrap;
        transition: color 0.2s ease;
    }

    .link:hover {
        color: var(--ink);
    }

    @media (min-width: 760px) {
        .top-nav {
            min-height: 68px;
            grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
            align-items: center;
            gap: 18px;
            padding: 14px clamp(24px, 4vw, 54px);
        }

        .brand {
            display: block;
            grid-column: 2;
            font-size: 1.34rem;
        }

        .links {
            grid-column: 3;
            justify-self: end;
            justify-content: flex-end;
            gap: clamp(13px, 2vw, 24px);
        }

        .link {
            font-size: 0.62rem;
        }
    }
</style>
